export const BAR_HEIGHT = 28;
/**
 * The dock bar is lifted into the preceding application surface.
 * An open panel must reserve the same amount below its host so the bar does not
 * cover the panel's lower boundary.
 */
export const BAR_OVERLAP = 8;
export const DOCK_Z_INDEX = 1100;
